package protocol

// version constants for protocol schema compatibility.
const (
	// SchemaVersion is the current FleetSnapshot JSON schema version.
	// Increment when the FleetSnapshot structure changes in a
	// backward-incompatible way.
	SchemaVersion = 1
)
